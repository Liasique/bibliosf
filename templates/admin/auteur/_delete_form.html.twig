<form method="post" action="{{ path('app_admin_auteur_delete', {'id': auteur.id}) }}" 
    onsubmit="return confirm('étes-vous sur(e) de vouloir suprimer cette auteur?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ auteur.id) }}">
    <button class="btn btn-danger">
    <i class="bi-trash"></i>
    Supprimer</button>
</form>

